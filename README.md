Real-time clock module (RTC). For connection to the CPU_8080 processor board.

Based on a real-time clock for RADIO-86RK.

Модуль часов реального времени (RTC). Для подключения к процессорной плате CPU_8080.

На базе часов реального времени для РАДИО-86РК. 
